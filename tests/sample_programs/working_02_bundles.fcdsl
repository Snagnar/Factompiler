# Working sample: Bundle operations 
# Tests: bundle(), bundle arithmetic, projections

Signal iron = input(0);
Signal copper = input(1);  
Signal coal = input(2);

# Create bundle with multiple channels
Bundle resources = bundle(iron, copper, coal);

# Bundle arithmetic (addition only - supported)
Bundle more_resources = resources + resources;

# Project specific channels from bundle
Signal iron_output = resources | "iron-plate";
Signal copper_output = resources | "copper-plate";
Signal coal_output = resources | "coal";
