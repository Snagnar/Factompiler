# Test 0: Wire merging behavior
# Tests: Multiple connections to the same signal, wire merging

Signal input1 = 50 | "signal-A";
Signal input2 = 30 | "signal-A";
Signal input3 = 20 | "signal-A";

Signal total_A = input1 + input2 + input3;

# Signal output = (input1 == 50) && (input2 == 20);