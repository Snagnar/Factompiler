# Working sample: Basic arithmetic and input/output
# Tests: input(), basic arithmetic (+, -, /), projections

Signal a = input(0);           
Signal b = input(1);          
Signal c = input(2);

Signal sum = a + b;            
Signal diff = c - b;          
Signal quotient = sum / 2;

# Output with explicit type projection
Signal output1 = sum | "signal-red";
Signal output2 = diff | "signal-green";
Signal output3 = quotient | "signal-blue";
