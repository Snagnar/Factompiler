# Working sample: Entity placement (basic)
# Tests: Place(), basic entity properties

# Place entities at specific coordinates
Entity lamp1 = Place("small-lamp", 0, 0);
Entity lamp2 = Place("small-lamp", 2, 0);

# Input controls
Signal enable_signal = input(0);

# Basic property assignment (simplified)
lamp1.enable = enable_signal;
lamp2.enable = enable_signal;
